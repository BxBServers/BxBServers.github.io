<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Block by Block - {{ name }}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">

    <link rel="stylesheet" href="/main.css">
</head>

<body>
    <section class="hero"
        style="background-image: linear-gradient( rgba(0, 0, 0, 0.25), rgba(0, 0, 0, 0.25) ), url(background.png);">
        <div>
            <h1>{{ name }}</h1>
            <h2>{{ date }}</h2>
        </div>
        <span class="hint">‸</span>
    </section>

    {%- if downloads %}
    <section class="text">
        {# <h2>Downloads</h2> #}
        <div class="downloads">
            {%- for download_name in downloads %}
            {%- set download = downloads[download_name] %}
            <div class="download">
                <h3><a href="{{ download.url }}" target="_blank" rel="noopener noreferrer">{{ download_name }}</a></h3>
                {%- if download.provider %}
                <p>Provided by {{ download.provider }}.</p>
                {%- endif %}
                <dl>
                    <dt>Version</dt>
                    <dd>{{ download.version }}</dd>
                    <dt>Hashes</dt>
                    <dd>
                        {%- for hash_kind in download.hashes %}
                        <a href="{{ download.hashes[hash_kind] }}" target="_blank" rel="noopener noreferrer">{{
                            hash_kind }}</a>
                        {%- endfor %}
                    </dd>
                    {%- if download.note %}
                    <dt>Note</dt>
                    <dd>{{ download.note }}</dd>
                    {%- endif %}
                </dl>
            </div>
            {%- endfor %}
        </div>
    </section>
    {%- endif %}
</body>

</html>